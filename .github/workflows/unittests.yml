name: Unittests
on:
  pull_request:
    branches: [ main ]
  # push:
  #   branches: [ main ]

jobs:
  unittests:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - run: cd docker && ./build.sh
    - run: docker run --rm cryo-em:latest python -m discover tests
